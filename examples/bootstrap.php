<?php
/**
 * Load Nette
 */

/*
$formBuilder = $container->getService('formBuilder.loader');
$config = __DIR__ . '/example.neon';
//$config = __DIR__ . '/register.neon';
$formFactory = $formBuilder->load($config);
$form = $formFactory->create(42, 24, 'some Parameter');

echo '<html><body>';
if($form->isSubmitted()) {
	$form->validate();
	dump($form->getValues());
}
echo((string)$form);
echo '</body></html>';
exit;
*/
